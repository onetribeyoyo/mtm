<div class="section float-left">
  <div>
    <g:link action="${action}" id="${project.id}"><button>Add <em>${dimensionName}</em> Dimension</button></g:link>
  </div>
  <p class="narrow hint">
    This will configure a "<em>${dimensionName}</em>" dimension with elements ${dimensionData.elements}.
  </p>
</div>
